import { SectionWithContainer } from "@/components";
import Form from "./component/Form";

const page = () => {
    return (
        <SectionWithContainer>
            <Form/>
        </SectionWithContainer>
    );
}

export default page;